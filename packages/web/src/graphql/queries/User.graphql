query User($id: String!) {
  user: User_by_pk(id: $id) {
    id
    avatar
    name
    introduction
    interested_in
    UserSkills {
      Skill {
        id
        avatar
        name
      }
    }
  }
}
