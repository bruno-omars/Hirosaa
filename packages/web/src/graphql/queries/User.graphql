query User($id: String!) {
  user: users_by_pk(id: $id) {
    id
    avatar
    name
    introduction
    interested_in
    user_skills {
      skills {
        id
        avatar
        name
      }
    }
  }
}
