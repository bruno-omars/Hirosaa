mutation InsertCircle($name: String, $mainRole: String, $whatWeWillDo: String) {
  insert_Circle(
    objects: {
      name: $name
      main_role: $mainRole
      what_we_will_do: $whatWeWillDo
    }
  ) {
    affected_rows
  }
}
