mutation InsertCircle(
  $name: String
  $recruitTitle: String
  $mainRole: String
  $whatWeWillDo: String
) {
  insert_Circle(
    objects: {
      name: $name
      recruit_title: $recruitTitle
      main_role: $mainRole
      what_we_will_do: $whatWeWillDo
    }
  ) {
    affected_rows
  }
}
