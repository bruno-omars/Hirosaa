subscription Messages($circleId: Int!) {
  Message(
    where: { Circle: { id: { _eq: $circleId } } }
    order_by: { timestamp: asc }
  ) {
    timestamp
    text
    id
    User {
      id
      avatar
      name
    }
  }
}
