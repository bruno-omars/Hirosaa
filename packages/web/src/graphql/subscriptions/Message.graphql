subscription Messages($circleId: Int!) {
  messages(
    where: { circles: { id: { _eq: $circleId } } }
    order_by: { timestamp: asc }
  ) {
    timestamp
    text
    id
    users {
      id
      avatar
      name
    }
  }
}
